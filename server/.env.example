NODE_ENV=development
PORT=5000
MONGO_URI=your_mongodb_connection_string
JWT_SECRET=your_jwt_secret
CLIENT_URL=http://localhost:5173
FRONTEND_URL=http://localhost:5173

# ============================================
# EMAIL DELIVERY (use ONE of the three options)
# ============================================
#
# Option 1: Google OAuth2 (RECOMMENDED for Render production)
#
#   Nodemailer OAuth2 with automatic token refresh. Most reliable on Render.
#
#   1. Go to https://console.cloud.google.com/apis/credentials
#   2. Create an OAuth 2.0 Client ID (type: Web application)
#   3. Add https://developers.google.com/oauthplayground as authorized redirect
#   4. Copy the Client ID and Client Secret
#   5. Go to https://developers.google.com/oauthplayground
#   6. Click the gear icon -> check "Use your own OAuth credentials"
#   7. Paste your Client ID and Client Secret
#   8. In Step 1, select https://mail.google.com/ scope
#   9. Click "Authorize APIs" and sign in with your Gmail account
#   10. Click "Exchange authorization code for tokens"
#   11. Copy the "Refresh token" value
#   12. Set the four variables below in Render Dashboard
#
EMAIL_USER=your_gmail_address@gmail.com
GOOGLE_CLIENT_ID=your_oauth2_client_id
GOOGLE_CLIENT_SECRET=your_oauth2_client_secret
GOOGLE_REFRESH_TOKEN=your_oauth2_refresh_token
#
# Option 2: Gmail App Password (fallback)
#
#   1. Enable 2-Factor Authentication on your Gmail account
#   2. Go to https://myaccount.google.com/apppasswords
#   3. Generate an App Password for "Mail"
#   4. Copy the 16-character password (spaces are optional)
#
# EMAIL_USER=your_gmail_address@gmail.com
# EMAIL_PASS=your_gmail_app_password
#
# Option 3: Generic SMTP provider (SendGrid, Mailgun, etc.)
#
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your_smtp_username
# SMTP_PASS=your_smtp_password
# SMTP_FROM="Elegant Home Decor <noreply@yourdomain.com>"

# ============================================
# EMAIL NOTIFICATIONS
# ============================================
# The sender "name" and "from" address used in all emails.
# Falls back to EMAIL_USER if not set.
MAIL_FROM_NAME=Elegant Home Decor
MAIL_FROM=no-reply@eleganthomedecor.com

# Admin notification recipients (comma-separated for multiple)
ADMIN_EMAIL=admin@eleganthomedecor.com
# EMAIL_ALERT_TO=admin@eleganthomedecor.com,manager@eleganthomedecor.com
LOW_STOCK_THRESHOLD=5

# ============================================
# PRODUCTION DEPLOYMENT (Render + Vercel)
# ============================================
# When deploying on Render, set ALL these in the Render Dashboard:
#   Dashboard -> Your Service -> Environment -> Environment Variables
#
# REQUIRED for production:
#   MONGO_URI                  - MongoDB Atlas connection string
#   JWT_SECRET                 - Random 64-char hex string
#   EMAIL_USER                 - Your Gmail address
#   GOOGLE_CLIENT_ID           - OAuth2 client ID (for OAuth2 auth)
#   GOOGLE_CLIENT_SECRET       - OAuth2 client secret
#   GOOGLE_REFRESH_TOKEN       - OAuth2 refresh token
#   ADMIN_EMAIL                - Email to receive order/admin notifications
#   CLIENT_URL                 - https://your-vercel-app.vercel.app
#   CLOUDINARY_CLOUD_NAME, CLOUDINARY_API_KEY, CLOUDINARY_API_SECRET
#   STRIPE_SECRET_KEY (if using Stripe)
#
# Also set in Vercel (frontend) dashboard:
#   VITE_API_URL      - https://your-render-app.onrender.com/api
#                       (MUST include /api suffix)
#
# Auth priority: SMTP_HOST > OAuth2 > App Password > disabled.
# If OAuth2 env vars are present, they are used. EMAIL_PASS is ignored
# unless OAuth2 vars are missing.
#
# Default SMTP port for Gmail is 587 with STARTTLS (requireTLS: true).
# Override with EMAIL_SMTP_PORT=465 for implicit SSL, or any other port.
# When overriding port, also set EMAIL_SECURE=true/false as needed.

# Payments and uploads
RAZORPAY_KEY_ID=your_razorpay_key_id
RAZORPAY_KEY_SECRET=your_razorpay_key_secret
STRIPE_SECRET_KEY=your_stripe_secret_key
CLOUDINARY_CLOUD_NAME=your_cloudinary_cloud_name
CLOUDINARY_API_KEY=your_cloudinary_api_key
CLOUDINARY_API_SECRET=your_cloudinary_api_secret
